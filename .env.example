# Mesh Environment Variables
# Copy this to .env and edit as needed

# Server Config
PORT=8080
PUBLIC_URL=https://trymesh.chat

# Security
# Optional: protect admin endpoints with a secret
ADMIN_CLAIM_SECRET=your-secret-here
# Optional: restrict CORS origins (comma-separated)
# ALLOWED_ORIGINS=https://trymesh.chat
# Optional: global API secret (all requests require this token)
# MESH_SECRET=your-api-secret

# Analytics (optional — only injected if set)
# POSTHOG_KEY=phc_...

# Room Config
# Optional: pre-create rooms on startup (comma-separated)
DEFAULT_ROOMS=mesh01
# Optional: set a password for default rooms
# ADMIN_ROOM_PASSWORD=your-room-password
